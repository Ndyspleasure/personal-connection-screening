{
  "name": "@pcs/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests --exclude '**/*.integration.*'",
    "test:integration": "vitest run --passWithNoTests integration"
  },
  "dependencies": {
    "@pcs/db": "workspace:*",
    "@pcs/security": "workspace:*",
    "@pcs/types": "workspace:*"
  },
  "devDependencies": {
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
