{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {},
    "test:integration": {
      "cache": false,
      "passThroughEnv": ["DATABASE_URL", "APP_ENV"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
